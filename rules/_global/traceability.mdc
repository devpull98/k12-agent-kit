---
description: Quy định về liên kết vết Traceability (implements & verifies) giữa Code, Test và BDD Scenario
globs: src/**/*, app/**/*, internal/**/*, lib/**/*, packages/**/*, services/**/*, modules/**/*, api/**/*
---

# Traceability — Quy tắc liên kết artifact → code → test

Không áp dụng cho: fast-track, hotfix, refactor nội bộ, optimize không đổi behavior.

## @trace tags (chỉ áp dụng khi có BDD spec chính thức)

### Trong code (implementation)
```
// @trace.implements: {UC-ID}-SC{N}
// @trace.implements: LOGIN-SC1
```
Đặt ở đầu class, method, hoặc handler thực hiện scenario đó.

### Trong test
```
// @trace.verifies: {UC-ID}-SC{N}
// @trace.verifies: LOGIN-SC1
```
Đặt ở đầu test class hoặc test method tương ứng.

## UC-ID convention
`{DOMAIN}-{TICKET}` hoặc tự đặt ngắn gọn, nhất quán trong project.
Ví dụ: `LOGIN-01`, `LMS-SCHEDULE-02`, `CLUB-QUEST-01`.

## Scenario numbering
SC1, SC2, SC3... theo thứ tự scenario trong .feature file.
Nếu 1 scenario được split thành nhiều test → vẫn dùng cùng SC-ID.

## Trace file (tùy chọn cho project lớn)
Lưu tại `docs/trace/{UC-ID}-trace.tsv`. Schema 10 cột (TAB-separated), header bắt buộc:
```
uc_id       scenario_id  spec_file                          implements_tag   verifies_tag     dev_selftest  qc_status  last_updated  owner   note
UC-LMS-001  SC1          docs/specs/bdd/UC-LMS-001.feature   UC-LMS-001-SC1   UC-LMS-001-SC1   pass          pass       2026-07-03    Dev+QA  Happy path
UC-LMS-001  SC2          docs/specs/bdd/UC-LMS-001.feature   UC-LMS-001-SC2   -                pass          -          2026-07-03    Dev     Chưa có test verify
```
Gate: có `implements_tag` (≠ `-`) → `dev_selftest` phải `pass`; có `verifies_tag` (≠ `-`) → `qc_status` phải `pass`.

## Validation
- Local/CI: `bash scripts/validate-trace.sh`
- Merge gate: `./scripts/governance-check.sh` (bao gồm trace validation)

## Quy tắc bất biến
- Chỉ thêm @trace khi UC-ID đến từ BDD spec đã được duyệt — không tự đặt UC-ID giả.
- Code có BDD spec mà không có @trace.implements → agent hỏi scenario nào đang implement.
- Test không có @trace.verifies → không tính là "covered" trong qc-automation (chỉ áp dụng khi có spec).
- Khi BDD spec thay đổi → tìm tất cả @trace.implements của UC đó, đánh giá impact trước khi merge.
