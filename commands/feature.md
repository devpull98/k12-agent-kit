---
description: Bắt đầu flow feature mới (brainstorm → spec → plan → tdd → review → ship)
---

Đọc `workflows/feature.md` để biết flow hiện tại đang ở bước nào.
Với mỗi step, dùng `router.yaml` để xác định skill cần load (xem `AGENTS.md` mục "Cách agent chọn skill").
Không tự nhảy bước, không load skill ngoài flow trừ khi user yêu cầu rõ.
